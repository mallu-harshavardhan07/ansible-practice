- name: printing ansbile facts
  hosts: local
  #connection: local
  tasks:  
  - name: task 1
    ansible.builtin.debug:
      msg: " printing ansible facts {{ansible_facts}}"
  - name: task2
    ansible.builtin.debug:
      msg: " printing os name{{ansible_facts['os_family']}}"